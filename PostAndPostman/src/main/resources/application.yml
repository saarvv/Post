server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_postman
    username: postgres
    password: sarvin1381


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update